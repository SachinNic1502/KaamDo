# Module inventory

Source-backed inventory. W = web/admin; M = the single mobile application. Status describes functional integration, not just whether files exist. No business module is certified Complete without runtime acceptance tests. Screen-level details are in UI/mobile audits; every row below includes its API and model chain.

| Module / roles / platform | Pages or screens | APIs → models | Current functionality / status | Missing, broken, UI/API/security concerns → work |
| --- | --- | --- | --- | --- |
| Authentication / all / W+M | landing/Header; Login/Otp; AppNavigator | auth → User + Redis | Password/OTP handlers, SecureStore restore; **Broken** | No SMS, new-user schema failure, compile errors, no web login, stale sessions → 005–008,014,022 |
| Admin shell / admin / W | admin/layout, Sidebar | no principal validation | Nested links, reusable layout; **Partially Complete** | Missing linked pages, no role guard, inert logout, fixed sidebar → 014,020 |
| Dashboard / admin / W | admin/page | analytics → User/WorkerProfile/Job/Payment/Payout/Dispute | DB aggregates and loading/error UI; **Partially Complete** | Recent-job/KYC sections unsupported, static action handlers, polling; no fake aggregate claim → 015,025 |
| Customer home / customer / M | customer/Home | categories/jobs → ServiceCategory/Job | Real queries and navigation; **Needs Refinement** | First-page data, missing error/retry, identity mismatch → 009,021 |
| Users / admin / W | admin/users | users GET/PATCH → User | Search/role/pagination read path; **Partially Complete** | Add/detail/activate/suspend menus inert; hash projection, schema updates → 008,016 |
| Worker discovery / customer/admin / W+M | customer/Search; services/kyc | workers → User/WorkerProfile | Search/status/skill query; **Broken** for filters/KYC | Public private fields; ignored price/rating; incompatible screen mappings and IDs → 001,004,010,016 |
| Worker onboarding/profile / worker / M | worker/Profile, Dashboard switch | workers POST/PATCH; users PATCH → WorkerProfile/User | Backend profile create, online mutation attempt; **Partially Complete** | No role enrollment/KYC screens; inert profile menus; user/profile state mixed; self-update bypass → 004,018,022 |
| Contractor management / admin/contractor / W | admin/users/contractors | none for profiles → ContractorProfile exists | Rendered static list; **Broken** | No real profile API or workflow; fake stats → 015,019 |
| Categories / admin/customer / W+M | services/categories; Home/CreateJob | categories CRUD → ServiceCategory | List/create with schema and invalidation; **Partially Complete** | Subcategory edit/delete UI gaps; inactive public data; reference-safe deletion → 010,016,024 |
| Job creation / customer / M | customer/CreateJob; admin Create button | jobs POST → Job/ServiceCategory/WorkerProfile | Five-step form, local image pick, backend create/match count; **Broken** | Payload field mismatch; no actual upload/assignment; client price input → 009,012,018 |
| Job lifecycle / customer/worker/admin / W+M | jobs page; both Jobs/JobDetail; worker Dashboard | jobs GET/PATCH → Job | Lists and status buttons; **Broken** | No detail contract, IDOR, no transition graph, OTP/money/field drift → 003,009,012,016 |
| Projects / customer/contractor/admin / W | jobs/projects | projects GET/POST/PATCH → Project/User | Lists, milestone storage/progress logic; **Partially Complete** | Inert create/detail UI; arbitrary mutation, no milestone payment invariant → 003,013,019 |
| Quotations / admin / W | jobs/quotations | jobs GET → Job | Filters current page by pricingModel; **Partially Complete** | No quote entity/submission/acceptance, wrong amount, totals span wrong rows → 019,021 |
| Payments / customer/admin / W+M | admin/payments; customer/JobDetail | payments → Payment/Job/Commission/Payout | Records and client SDK wrapper; **Broken** | No provider order/verification, fake settlement, no atomicity, no accurate ledger → 002,017,024 |
| Saved payment methods / owner / W intended | hook only; mobile profile item inert | payment-methods → PaymentMethod | Draft GET/POST/DELETE source; **Broken** | Missing export, nested schema mismatch, missing PATCH/auth in hook, delete IDOR → 003,005,011 |
| Earnings/payouts / worker/admin / W+M | worker/Earnings; admin payments tabs | payments GET → Payment; Payout not read | Page-based sums, no payout API; **Broken** | Lifetime balance shown from page; payout filtering on nonexistent type → 017,021 |
| Attendance / worker/customer/admin / W+M | admin/attendance; worker/JobDetail | attendance → Attendance/Job/User | Record storage, hours, approvals; **Broken** | Admin envelope crash; location actions mismatch; unguarded wages; no server geofence → 003,013,016 |
| Disputes / customer/worker/admin / W+M | admin/disputes; common/Dispute | disputes → Dispute/Job | Create/list/resolution backend skeleton; **Partially Complete** | No participant check; evidence/status mismatch; view/resolve UI inert; partial writes → 003,013,019 |
| Promotions / customer/admin / W+M | admin/promotions; common/Promo | promotions → PromoCode | List/create API; **Broken** end-to-end | Modal doesn't submit; validation action absent; names/active filters differ; copy alert doesn't copy → 019 |
| Chat / job participants / M | common/Chat | jobs GET action=messages; socket client → no Message model | UI/event listeners; **Broken** | Jobs returned as messages, no server authorization/storage/delivery acknowledgment → 023 |
| Notifications / all / M | common/Notifications, App initialization | users POST push token unsupported → no notification model | Permission/client listener code, static inbox; **Broken** | No durable inbox/send/retry/token ownership or deep links → 023 |
| Reviews / customer/worker / M | common/Rating; profile item | jobs POST action=rate; workers GET reviews → Job.rating | Form and unused PATCH rating capability; **Broken** | Wrong method/action; no verified-job ownership/aggregation → 012,019 |
| Settings / admin/owner / W+M | admin/settings; common/Settings | no admin settings endpoint; local thunk → User.notificationSettings unused | Static web controls, local preference action; **Broken** | Save not persisted, mobile compile/event issues → 005,019 |
| Upload/location / participants / M | CreateJob/Dispute/worker JobDetail | Cloudinary or unsupported users POST; attendance actions | Picker, GPS, maps links; **Partially Complete** | No authenticated upload backend/private KYC; local URIs; timers/lifecycle → 018,022 |
| Shop/retailer app / unspecified | None | None | **Missing** | No shop role/models/routes. Decide separate product scope; do not pretend contractor tabs exist → 031 |
| Shared contracts/state/UI / developers / W+M | types, hooks, providers, UI wrappers, constants | duplicated fetch and DTO definitions | Reusable primitives within each app; **Needs Refinement** | No shared schemas, cache isolation, central permissions; no-op Redux and dead helpers → 020,022,026,032 |
| Operations/audit / operators / backend | config, seed script | no audit collection/health route | Env sample, scripts; **Partially Complete** | No CI/release controls, monitoring, backup validation, durable jobs → 027–030 |

Deprecated/Unused candidates: `mockJobs`, unused middleware/cache/session helpers and unattached hook exports are documented in dead-code-report.md. They are not separate complete business modules.
